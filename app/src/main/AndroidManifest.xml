<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sherpalink"
        android:usesCleartextTraffic="true"
        tools:replace="android:fullBackupContent"
        tools:targetApi="31">

        <!-- Admin Launcher Activity (for testing) -->
        <activity
            android:name=".admin.AdminLauncherActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AuthCheckActivity (normal flow) -->
        <activity
            android:name=".auth.AuthCheckActivity"
            android:exported="true"
            android:theme="@style/Theme.Sherpalink" />

        <!-- Auth activities -->
        <activity android:name=".auth.SignUpActivity" android:exported="true" />
        <activity android:name=".auth.SignInActivity" android:exported="true" />

        <!-- Main app dashboard -->
        <activity android:name=".DashboardActivity" android:exported="false" />

        <!-- Google Maps API key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_ACTUAL_API_KEY" />

    </application>
</manifest>
